# CONTEXT.md — 日报守卫 (DailyReport Guard)

## 领域术语

### 日报 (Daily Report)
钉钉工作台"日志"应用中的日报。用户在一个自然日内按指定日志模板提交的工作汇报。钉钉平台内支持补交（过去的日期也可提交）。

### 钉钉日志模板 (DingTalk Report Template)
钉钉企业内创建的日志模板（如"工作日报"）。每个模板有唯一的 `report_id`。用户可以选择一个或多个模板让应用盯。当前只有一个模板，采用 AND 逻辑（如选了多个模板，所有模板都必须提交才算当日"已写"）。

### 工作日 (Workday)
中国法定工作日：周一至周五，排除国务院公布的法定节假日，包含调休补班日（某个周六/周日调整为上班日）。节假日数据来源为外部 API。

### 缺失 (Missing)
在一个过去的工作日，用户在指定模板上没有提交任何日志。只有过去日期才能被判定为缺失——当日和未来日期不算缺失。

### 预警 (Warning)
当天下午 17:30 触发的提醒，仅针对"今天还没写"这一情况。预警不算最终缺失判定，只是提醒用户"还有时间，快去写"。日历上以橙色 ⚠️ 标记。

### 确认 (Confirmation)
次日 09:30 触发的最终检查，针对昨天及以前的所有本月工作日。如果确实没有日报记录，判定为"缺失"。日历上以红色 ❌ 标记。

### 已写 (Submitted)
用户在该工作日的指定模板上至少提交了一篇日志。日历上以绿色 ✅ 标记。

### 工作通知 (Work Notice)
钉钉企业内部应用通过 `topapi/message/corpconversation/asyncsend_v2` 接口向指定用户推送的消息，展示在钉钉工作助手的"工作通知"中。日报守卫通过此渠道发送缺失提醒。

### 钉钉企业内部应用 (DingTalk Internal App)
用户在钉钉开放平台创建的企业内部应用，通过 AppKey + AppSecret 获取 access_token，调用钉钉 Open API。

### 检查引擎 (Check Engine)
应用的核心逻辑：获取本月工作日列表 → 通过钉钉 statistics API 获取用户已提交日志的日期 → 差集得出缺失日期 → 区分"预警"和"确认" → 发送工作通知 + 更新界面。

### 检查月 (Check Month)
当前自然月。应用仅检查和展示当前月的数据，不做历史月份的检查。每月 1 日跨月时，昨日确认检查跳过上月最后一天（严格只看当月）。

### 日报内容 (Report Content)
日报的具体填写内容。包含两个字段：**日报日期**（指定该日报属于哪一天）和**工作内容**（日报正文）。通过钉钉 API 获取或创建。

### 创建/补交 (Create / Backfill)
在应用中为某个缺失日期编写日报内容，调用钉钉 API 提交到钉钉日志系统。提交成功后该日期状态自动变为"已写"。

### 已写日查看 (View Submitted)
点击已写日期的单元格，弹窗展示该日日报的详细内容，只读不编辑。内容通过钉钉 API 实时获取。

## 关键边界条件

- **补交覆盖**：每次检查全量刷新本月状态，补交的日期自动从"缺失"变为"已写"。
- **未来日期**：不检查，日历上灰色标记。
- **API 失败**：重试 3 次后跳过，托盘图标显示警告 tooltip，下次检查恢复。
- **应用不在运行时**：启动时立即执行全量检查，定时器作为额外推动。
- **工作日数据降级**：节假日 API 不可用时降级为周一至周五判断。
